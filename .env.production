# ============================================
# 生产环境配置文件
# ============================================
# 注意：此文件包含敏感信息，不应提交到版本控制系统
# 实际部署时，请在部署平台（如 Vercel）的环境变量中设置这些值

# ============================================
# NextAuth 认证配置
# ============================================
# NextAuth 的基础 URL（生产环境域名）
NEXTAUTH_URL=https://tribe.vicong.xyz
# 或者使用 AUTH_URL（NextAuth 5.x 也支持）
AUTH_URL=https://tribe.vicong.xyz

# NextAuth 密钥（用于加密 JWT token）
# 生成方式：openssl rand -base64 32
# 必须设置一个强随机字符串
AUTH_SECRET=请替换为你的密钥

# ============================================
# 后端 API 配置
# ============================================
# 后端服务 URL（通过 Cloudflare 代理后的地址或直接后端地址）
# 如果后端也在同一域名下，可以使用相对路径或完整 URL
NEXT_PUBLIC_BACKEND_URL=https://tribe.vicong.xyz:3000
# 或者如果后端通过 Cloudflare 代理：
# NEXT_PUBLIC_BACKEND_URL=https://backend.tribe.vicong.xyz

# ============================================
# 数据库配置
# ============================================
# PostgreSQL 数据库连接字符串
# 格式：postgresql://用户名:密码@主机:端口/数据库名?schema=ai
POSTGRES_URL=postgresql://user:password@localhost:5432/ai?schema=ai

# ============================================
# 文件存储配置（MinIO/S3）
# ============================================
# MinIO 端点（如果使用 MinIO）
MINIO_ENDPOINT=http://localhost:9000
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_REGION=us-east-1
MINIO_BUCKET=tribe-files
MINIO_PUBLIC_URL=http://localhost:9000

# 或者使用 AWS S3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your_bucket_name

# ============================================
# 环境标识
# ============================================
NODE_ENV=production

# ============================================
# 可选配置
# ============================================
# 调试模式（生产环境建议关闭）
# DEBUG=false

# 日志级别
# LOG_LEVEL=info
